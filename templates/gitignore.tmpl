# OS
.DS_Store
Thumbs.db
ehthumbs.db
$RECYCLE.BIN/
*.swp

# Editors
.vscode/
.idea/
*.sublime-workspace

%{ if contains(languages, "terraform") }
# Terraform
.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl
%{ endif }

%{ if contains(languages, "python") }
# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
%{ endif }

%{ if contains(languages, "node") }
# Node.js
node_modules/
npm-debug.log*
yarn-error.log*
package-lock.json
%{ endif }

%{ if contains(languages, "go") }
# Go
*.test
*.out
bin/
%{ endif }

%{ if contains(languages, "java") }
# Java
*.class
*.jar
*.war
*.ear
hs_err_pid*
%{ endif }

%{ if contains(languages, "typescript") }
# TypeScript
*.tsbuildinfo
%{ endif }

%{ if contains(languages, "kotlin") }
# Kotlin
*.class
*.jar
*.war
*.ear
build/
out/
%{ endif }

%{ if contains(languages, "ruby") }
# Ruby
*.gem
*.rbc
tmp/
log/
bin/
coverage/
*.log
%{ endif }

%{ if contains(languages, "csharp") }
# C#
*.exe
*.dll
*.pdb
*.cache
*.config
*.csproj.user
%{ endif }

%{ if contains(languages, "cpp") }
# C++
*.o
*.obj
*.exe
*.out
*.a
*.so
*.dSYM/
%{ endif }

%{ if contains(languages, "php") }
# PHP
vendor/
*.log
%{ endif }

%{ if contains(languages, "rust") }
# Rust
target/
**/*.rs.bk
%{ endif }

%{ if contains(languages, "swift") }
# Swift
.build/
DerivedData/
*.xcworkspace
*.xcodeproj/project.xcworkspace/
%{ endif }

%{ if contains(languages, "perl") }
# Perl
*.pm.tdy
*.bs
blib/
Build
Build.bat
Makefile
Makefile.old
%{ endif }

%{ if contains(languages, "r") }
# R
.Rhistory
.RData
.Rproj.user/
.Rproj
*.Rproj
%{ endif }